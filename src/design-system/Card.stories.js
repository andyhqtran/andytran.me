import React from 'react';

import { Card } from 'design-system/Card';

export default {
  title: 'Design System/Card',
  component: Card,
};

export const preview = () => {
  return <Card height={300} />;
};
