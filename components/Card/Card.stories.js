import React from 'react'

import { Card } from 'components/Card'

export default {
  title: 'Core/Card',
  component: Card
}

export const preview = () => {
  return (
    <Card height={300} />
  )
}
