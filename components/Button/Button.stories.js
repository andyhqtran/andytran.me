import React from 'react'

import { Button } from 'components/Button'

export default {
  title: 'Core/Button',
  component: Button
}

export const preview = () => {
  return (
    <Button>
      View project ⟶
    </Button>
  )
}
